# Mirrors systempromptio/systemprompt-template/.cargo/config.toml.
# Mold linker pin removed — see that file's header comment.

[target.x86_64-unknown-linux-gnu]
linker = "clang"

[env]
SQLX_OFFLINE = { value = "true", force = false }
